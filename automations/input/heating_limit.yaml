---
- id: heating_limit_set
  alias: set heating when limits change
  mode: queued

  trigger:
    - platform: state
      entity_id: input_number.heating_minimum
    - platform: state
      entity_id: input_number.heating_maximum

  action:
    - service: script.presence_heating
